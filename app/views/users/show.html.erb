
<h2>Orders List </h2>

<% @user.orders.each do |order| %>
  <p>
    <strong>Order:</strong>
    Name:<%= order.name %><br>
    item:<%= order.res_name%>
  </p>
  <% end %>
  
<h1>Users</h1>
    <h1>Name-<%= @user.user_name %></h1>
    <h1>Email-<%= @user.user_email%></h1>
    <h1>Phone-<%= @user.user_phone%></h1>
    <h1>Address-<%=@user.user_address%></h1>

<a>
<%= link_to "Edit", edit_user_path(@user) %></li>
  <%= link_to "Delete", user_path(@user), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
</a>

